class Main {
    function void main(){
        var int exit;
        var GameManager gameManager;

        let exit = 0;
        let gameManager = GameManager.new();

        do gameManager.reset();
        do gameManager.gameScene();

        while(exit = 0){
            let exit = gameManager.nextTurn();
        }

        return;
    }
}