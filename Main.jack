class Main {

    var GameManager gameManager;

    function void main() {
        let gameManager = GameManager.new();

        gameManager.startScene();
        gameManager.reset();
        gameManager.gameScene();

        while(Updater.NextTurn()){
        }

        gameManager.endScene();

        return;
    }
    
}