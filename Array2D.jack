class Array2D {
    field int columnCnt;
    field Array data;

    constructor Array2D new(int rows, int cols) {
        var int index;
        let index = 0;

        let data = Array.new(rows * cols);
        let columnCnt = cols;
        while(~(index = rows * cols)){
            let data[index] = 0;
            let index = index + 1;
        }
        return this;
    }

    method int get(int row, int col) {
        return data[(row * columnCnt) + col];
    }

    method void set(int row, int col, int value) {
        let data[(row * columnCnt) + col] = value;
        return;
    }
}