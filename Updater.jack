class Updater {
    
    method void reset(){
        
    }

    method int NextTurn(){
        //ref: Character and ask if player want to turn the man
        //ref: Character to move the man

        //ref: Gamemanager to calc money gain

        //ref: CarpetPlacer to place the carpet

        //ref: Gamemanager to see if game end
        //if ended return 0 else return 1
    }
    
}